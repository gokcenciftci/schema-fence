openapi: 3.0.3
info:
  title: Cyclic reference API
  version: 1.0.0
paths:
  /items:
    get:
      responses:
        '200':
          description: Item
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/First'
components:
  schemas:
    First:
      $ref: '#/components/schemas/Second'
    Second:
      $ref: '#/components/schemas/First'
