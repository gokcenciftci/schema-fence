openapi: 3.0.3
info:
  title: Missing reference API
  version: 1.0.0
paths:
  /items:
    get:
      responses:
        '200':
          description: Item
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Missing'
components:
  schemas: {}
