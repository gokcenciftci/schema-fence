openapi: 3.0.3
info:
  title: External reference example
  version: 1.0.0
paths:
  /items:
    get:
      responses:
        '200':
          description: Items
          content:
            application/json:
              schema:
                $ref: './shared.yaml#/components/schemas/Item'
